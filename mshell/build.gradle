project.version = "0.0.0"

loom {
    javaVersion = 14
}

dependencies {
    modInclude("api-base")
    modInclude("api-lifecycle-events")
    modInclude("api-key-bindings")

    apiInclude(":cell")
}
