version = "0.0.0"

dependencies {
    modApi("${project.apiBase}")
    modApi("${project.apiLifecycleEvents}")
    modInclude("${project.apiKeyBindings}")

    api(include("${project.joor}"))

    includeLocal("cell")
}
